<%# locals: (entry:, entries:, index:, with_date: false, with_project: false, with_member: false, grouped: true) %>

<%# `grouped` shows a date / project / member only where it changes from the line
    above. The tag page turns it off, as its lines jump between days and members. %>

<%
  meta_columns = [ with_date, with_project, with_member ].count(true)
  first_of_group = ->(attribute) { !grouped || index.zero? || entry.public_send(attribute) != entries[index - 1].public_send(attribute) }
%>

<li class="nude" id="<%= dom_id(entry) %>">
<div class="entry-grid" style="<%= entry_grid_style(meta_columns:) %>">

  <% if with_date %>
    <div class="left">
      <% if first_of_group.call(:day) %>
        <h3 class="entry-group-heading">
          <%= link_to entry.day.date.iso8601, entries_path(date: entry.day.date, project_id: @project&.id), method: :get, class: "camouflage s-link" %>
        </h3>
      <% end %>
    </div>
  <% end %>

  <% if with_project %>
    <div class="left">
      <% if first_of_group.call(:project) %>
        <h3 class="entry-group-heading">
          <%= link_to entry.project.name, project_path(entry.day.project.slug), method: :get, class: "camouflage s-link" %>
        </h3>
      <% end %>
    </div>
  <% end %>

  <% if with_member %>
    <div class="left">
      <% if first_of_group.call(:member) %>
        <h3 class="entry-group-heading">
          <%= link_to entry.member.name, entries_path(member_id: entry.member_id, project_id: @project&.id), method: :get, class: "camouflage s-link" %>
        </h3>
      <% end %>
    </div>
  <% end %>

  <div class="left">
    <%= render_log(entry) %>
  </div>

  <!-- read-only status: the same three states as the editable entry, without the form -->

  <div class="right">
    <% Entry::STATES.each do |state| %>
      <span role="button" class="slim <%= entry.status == state ? "btn-#{state}" : 'btn-inactive' %>"><%= state %></span>
    <% end %>
  </div>

</div>
</li>
